# UI Visual Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复完成弹窗过宽、片段列表低对比度和未勾选设备复选框悬停时消失的问题，同时保留现有勾选动效。

**Architecture:** 所有生产修改集中在现有 `APP_STYLE` QSS 中，通过 Qt 对象名和控件子状态精确限定作用范围。测试先断言必要选择器不存在而失败，再用最小样式实现使其通过；不修改主窗口业务逻辑或创建自定义控件。

**Tech Stack:** Python 3.12、PySide6、Qt Style Sheets、pytest、现有 UI 截图脚本。

## Global Constraints

- 只修改本地功能分支，不推送 GitHub。
- 不改变转换流程、输出格式、片段模型或设备选择行为。
- 不覆盖 `QCheckBox::indicator:checked`，保留当前原生勾选效果和动效。
- 不增加第三方依赖。
- 视觉核验使用 Windows 深色调色板，以复现用户截图中的环境。

---

### Task 1: Add style regression coverage

**Files:**
- Modify: `tests/test_ui.py:197`

**Interfaces:**
- Consumes: `livephoto.ui.theme.APP_STYLE: str`
- Produces: `_style_rule(selector: str) -> str` 测试辅助函数，以及三个局部样式回归测试。

- [ ] **Step 1: Extract the style-rule test helper**

```python
def _style_rule(selector: str) -> str:
    match = re.search(rf"{re.escape(selector)}\s*\{{([^}}]+)\}}", APP_STYLE)
    assert match is not None, f"缺少样式规则：{selector}"
    return match.group(1)
```

将现有 `test_message_boxes_have_explicit_high_contrast_colors` 改为调用 `_style_rule`，保持原断言不变。

- [ ] **Step 2: Write failing tests for all three reported states**

```python
def test_message_box_labels_do_not_force_the_icon_column_wide():
    generic_label = _style_rule("QMessageBox QLabel")
    message_label = _style_rule("QMessageBox QLabel#qt_msgbox_label")
    icon_label = _style_rule("QMessageBox QLabel#qt_msgboxex_icon_label")

    assert "min-width" not in generic_label
    assert "min-width: 0px" in message_label
    assert "min-width: 0px" in icon_label


def test_segment_list_has_readable_normal_hover_and_selected_states():
    list_rule = _style_rule("QListWidget#segmentList")
    item_rule = _style_rule("QListWidget#segmentList::item")
    hover_rule = _style_rule("QListWidget#segmentList::item:hover")
    selected_rule = _style_rule("QListWidget#segmentList::item:selected")

    for rule in (list_rule, item_rule, hover_rule, selected_rule):
        assert "color:" in rule
    assert "background:" in list_rule
    assert "background:" in hover_rule
    assert "background:" in selected_rule


def test_unchecked_checkbox_indicator_remains_visible_on_hover():
    unchecked = _style_rule("QCheckBox::indicator:unchecked")
    hover = _style_rule("QCheckBox::indicator:unchecked:hover")

    assert "border:" in unchecked
    assert "background:" in unchecked
    assert "border:" in hover
    assert "background:" in hover
    assert "QCheckBox::indicator:checked" not in APP_STYLE
```

- [ ] **Step 3: Run the focused tests and verify RED**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_ui.py -k "message_box_labels or segment_list_has_readable or unchecked_checkbox" -q
```

Expected: three failures reporting missing QSS rules or the generic message-label `min-width`.

- [ ] **Step 4: Commit the failing tests together with the approved design and plan**

```powershell
git add tests\test_ui.py docs\superpowers\specs\2026-07-18-ui-polish-design.md docs\superpowers\plans\2026-07-18-ui-polish.md
git commit -m "test: define UI polish regressions"
```

### Task 2: Implement the minimal targeted QSS fixes

**Files:**
- Modify: `livephoto/ui/theme.py:61-73`
- Test: `tests/test_ui.py`

**Interfaces:**
- Consumes: Qt internal message-label object names `qt_msgbox_label` and `qt_msgboxex_icon_label`, plus existing object name `segmentList`.
- Produces: updated `APP_STYLE` with no Python API changes.

- [ ] **Step 1: Add explicit segment-list states**

```css
QListWidget#segmentList {
    background: #f9fbfd; color: #263650; border: 1px solid #cfd9e7;
    border-radius: 8px; outline: none; padding: 5px;
}
QListWidget#segmentList::item {
    min-height: 30px; padding: 3px 8px; color: #263650; border-radius: 5px;
}
QListWidget#segmentList::item:hover { background: #edf5ff; color: #17233c; }
QListWidget#segmentList::item:selected { background: #dcecff; color: #10213d; }
```

- [ ] **Step 2: Add only unchecked checkbox-indicator states**

```css
QCheckBox::indicator:unchecked {
    width: 16px; height: 16px; border: 1px solid #aab8cb;
    border-radius: 4px; background: #f9fbfd;
}
QCheckBox::indicator:unchecked:hover {
    border: 2px solid #1769e0; background: #edf5ff;
}
```

- [ ] **Step 3: Narrow the message-label sizing rules**

```css
QMessageBox QLabel { color: #17233c; background: transparent; }
QMessageBox QLabel#qt_msgbox_label { min-width: 0px; }
QMessageBox QLabel#qt_msgboxex_icon_label { min-width: 0px; }
```

- [ ] **Step 4: Run the focused tests and verify GREEN**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_ui.py -k "message_box or segment_list_has_readable or unchecked_checkbox" -q
```

Expected: all selected tests pass.

- [ ] **Step 5: Run the complete UI test module**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_ui.py -q
```

Expected: all UI tests pass.

- [ ] **Step 6: Commit the style fix**

```powershell
git add livephoto\ui\theme.py
git commit -m "fix: improve UI state contrast"
```

### Task 3: Verify the rendered UI and complete regression suite

**Files:**
- No production file changes expected.
- Reuse: `scripts/capture_ui.py`

**Interfaces:**
- Consumes: the application entry point and `scripts.capture_ui` screenshot options.
- Produces: local PNG evidence and fresh test/build results; generated verification artifacts remain untracked.

- [ ] **Step 1: Capture the main UI under the dark Windows palette**

```powershell
.\.venv\Scripts\python.exe scripts\capture_ui.py --output .artifacts\ui-polish-main.png
```

Expected: the segment list is light with dark text, and every unchecked device indicator remains visible.

- [ ] **Step 2: Capture the completion dialog**

```powershell
.\.venv\Scripts\python.exe scripts\capture_ui.py --dialog --output .artifacts\ui-polish-dialog.png
```

Expected: the dialog has readable dark text on a light background and no oversized gap between icon and message.

- [ ] **Step 3: Run the full automated test suite**

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

Expected: all tests pass.

- [ ] **Step 4: Check the final local diff and repository state**

```powershell
git diff HEAD~2 --check
git status --short --branch
```

Expected: `git diff --check` exits successfully; the feature branch is clean except for intentionally untracked local verification artifacts ignored by `.gitignore`.

